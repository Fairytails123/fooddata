.claude/
# Local test asset — real customer names + medication details; never publish to Pages
live_api_sample.json
# Backend source — local-only MIRROR OF THE LIVE Apps Script (contains Acuity/JotForm
# API keys; this repo is PUBLIC + Pages-served). Deploys go via clasp, never via this repo.
supersetplanner&feed.gs

.task/

.gate-evidence/
