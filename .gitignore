.claude/
# Local test asset — real customer names + medication details; never publish to Pages
live_api_sample.json
# Backend source — local-only MIRROR OF THE LIVE Apps Script (secret-free since
# 2026-08-10: creds moved to Script Properties). Stays ignored because the live script
# is the source of truth and deploys go via clasp (Boardingplan repo CI), never via here.
supersetplanner&feed.gs

.task/

.gate-evidence/
